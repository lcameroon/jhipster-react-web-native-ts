import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  appHeader: {
    flex: 1,
    backgroundColor: '#222',
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center'
  },
  appTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white'
  }
});
